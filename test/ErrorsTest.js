/**
 * Test for Errors.
 * Runs with mocha.
 */
'use strict'

const Errors = require('../lib/Errors')
const { ok, equal } = require('assert')

describe('errors', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
